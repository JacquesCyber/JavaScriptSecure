# MongoDB Connection
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority

# Encryption Keys (Generate your own!)
ENCRYPTION_KEY=your-32-character-encryption-key  # Must be exactly 32 characters for AES-256
ENCRYPTION_IV=your-16-char-iv                    # Must be exactly 16 characters

# SonarQube (Optional)
SONARQUBE_TOKEN=your-sonarqube-token

# Session & JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-minimum-256-bits-for-production-use-only
SESSION_SECRET=your-super-secret-session-key-for-secure-cookie-signing-production

# Test Employee Credentials (Development/Demo Only - Change in Production!)
TEST_EMPLOYEE_PASSWORD=YourSecurePassword123!
TEST_EMPLOYEE_USERNAME=employee001
TEST_EMPLOYEE_ID=EMP001234
TEST_EMPLOYEE_EMAIL=test.employee@securebank.com

# Application Settings
NODE_ENV=development
PORT=3000
